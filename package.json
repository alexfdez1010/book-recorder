{
  "name": "book-recorder",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "packageManager": "bun@1.3.12",
  "scripts": {
    "dev": "bun run database && next dev --turbopack",
    "build": "prisma generate && next build",
    "start": "next start",
    "postinstall": "prisma generate",
    "lint": "eslint . && bun run database:format",
    "launch": "bun run database && bun run database:deploy && bun run build && bun run start",
    "launch:test": "bun run database:test && bun run build && bun run start && bun run database:down",
    "format": "bunx prettier . --write",
    "lint-format": "bun run lint && bun run format",
    "pre-commit": "bun run test && bun run lint-format",
    "test": "bun run test:unit && bun run test:integration && bun run test:e2e",
    "test:unit": "vitest run --dir tests/unit",
    "test:integration": "vitest run --dir tests/integration --disable-console-intercept",
    "test:e2e": "bun run database:test && bun run build && start-server-and-test start http://localhost:3000 'bunx playwright test' && bun run database:down",
    "test:specific": "bunx playwright test -g",
    "database:dev": "bun run database && prisma migrate dev",
    "database:deploy": "bun scripts/migrate-deploy.ts",
    "database:check": "prisma migrate diff --to-schema-datamodel prisma/ --from-schema-datasource prisma/ --exit-code",
    "database": "docker compose up -d",
    "database:test": "docker compose -f compose-test.yml down -v && docker compose -f compose-test.yml up -d && bun run database:create",
    "database:down": "docker compose -f compose-test.yml down -v",
    "database:create": "prisma generate && prisma migrate deploy",
    "database:studio": "bun run database && bunx prisma studio",
    "database:format": "prisma format",
    "playwright": "bunx playwright test --ui",
    "vercel-build": "bun run build && if [ \"$VERCEL_ENV\" = \"production\" ]; then bun run database:deploy; fi"
  },
  "dependencies": {
    "@heroui/react": "^3.2.4",
    "@heroui/styles": "^3.2.4",
    "@prisma/client": "^6.7.0",
    "clsx": "^2.1.1",
    "dotenv": "^17.2.3",
    "lucide-react": "^1.8.0",
    "mcp-handler": "^1.1.0",
    "next": "^16.0.7",
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "recharts": "^2.15.0",
    "tailwind-merge": "^3.5.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@playwright/test": "^1.55.1",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.5.4",
    "prisma": "^6.16.3",
    "start-server-and-test": "^2.1.2",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^3.2.4"
  }
}
