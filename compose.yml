services:
  db:
    image: postgres:16-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=book_recorder
    container_name: postgres_book_recorder
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    ports:
      - '5434:5432'
